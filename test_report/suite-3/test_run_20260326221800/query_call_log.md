# test_run_20260326221800 — `kweaver dataview query` 调用计数（边跑边记）

| 阶段 | 次数 | 说明 |
|------|------|------|
| S1 | 23 | 含列名试错（forecast `material_code`、PO `billno`、supplier `avg_lead_time_days`）与后台批次中未完整采集后补跑 |
| S2 | 17 | 仅本卷新增查询；与 S1 重复的口语题未再查 |
| S3 | 20 | 含 Q07/Q08 单独补跑 |
| S4 | 13 | 只读边界查询 |
| S5 | 2 | 缺料合计、`SUP-021`；其余复用 S1 |
| **合计** | **75** | |

`sql_chars_total`：未逐字符落盘；可按本目录各卷内 SQL 原文累加（或按会话中提交的 `--sql` 字符串长度求和）。粗算可按 **~75 次 × 平均 SQL 长度** 估算。
